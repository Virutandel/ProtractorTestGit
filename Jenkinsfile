pipeline {
    // Limit this to run on only some machines
    agent { label "D16StaticSlave72" }

node{
    stage('Compile-Package'){
      	sh """ 
                # Run test
               
               protractor conf.js    }
        }
}
